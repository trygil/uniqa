<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Hello Adonis</title>
    {{ css('assets/admin_application_style.css') }}
</head>
<body>
    <section>
        @if(flashMessage('success'))
            @each(msg in flashMessage('success'))
                <el-alert title="success alert" type="success">{{ msg }}</el-alert>
            @endeach
        @endif

        <div class="logo"></div>
        <div class="title"></div>
        <div class="subtitle">
            <p>AdonisJs simplicity will make you feel confident about your code</p>
        </div>
    </section>

    {{ script('assets/admin_application.js') }}
</body>
</html>
