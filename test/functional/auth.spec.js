// 'use strict'

// const { test, trait } = use('Test/Suite')('Auth')

// trait('Test/Browser')

// test('make sure error login is showing', async ({ browser }) => {
//   const page = await browser.visit('/')

//   // console.log(await page.getHtml());

//   await page.click("nav .menu .btn")
//   await page
//     .type("#login-panel [aria-label='Email']", "invalid user")
//     .type("#login-panel [aria-label='Password']", "invalid password")
//   await page.click("#login-panel .btn")
//     // .waitForNavigation()

//   await page.assertHas("your username or password wrong")

//   // #inspire > div.application--wrap > nav > div > div:nth-child(4) > div:nth-child(1) > div > button
//   // await page.assertHas('Adonis')

//   // assert.equal(2 + 2, 4)
//   // your username or password wrong
// }).timeout(0)
